# Product Readiness

Swain's target user is a solo builder or small team shipping a real app fast.
The product should feel like a pragmatic security lead in the repo: direct,
specific, low-friction, and useful even when Claude or Codex quota is unavailable.

## Current Experience Bar

- First launch runs setup before the TUI, explains what Swain reads/writes, and
  lets the user choose Claude CLI, Codex CLI, or hybrid mode.
- Findings include a short ID so `/fix <id>` and `/feedback <id> fp` are obvious.
- Scan summaries include a next step, not just a pass/fail result.
- `swain status` ranks the latest open findings, names the fix-first item, and
  prints exact CLI commands for a patch draft or false-positive feedback.
- Local fallback answers handle "what should I fix first?" and launch-readiness
  questions without needing an LLM call.
- README explains the first-run TUI flow and the launch-risk surfaces Swain covers.
- Public install is one command: `curl ... | sh`, then `swain` inside a repo.
- `swain doctor` gives a preflight view of repo setup, playbooks, worker
  availability, quota/auth probes, and package hygiene.
- Generated Python bytecode is ignored and no longer tracked as package content.
- Full configured lint now passes across `scripts`, `src`, `tests`, and
  `examples`; the current test suite runs without warnings.
- Built-in playbooks have routing/prompt regression coverage against a fixture
  SaaS app with auth, payments, uploads, SQL/data access, XSS, and tenant risks.
- Wheel packaging includes bundled playbooks, schemas, and the public demo, so
  installed scans can load the same checks and demo as the source checkout.
- First-run Textual smoke coverage verifies the TUI mounts, greets a fresh repo,
  and answers launch-readiness locally.
- A release demo script exists for the preflight, first-run chat, scan,
  fix-draft, and feedback loop.
- A release checklist captures automated gates, CLI smoke checks, real-worker
  fallback checks, TUI smoke checks, and demo asset requirements.
- `examples/launchpad-saas` provides a public, initialized React + FastAPI demo
  repo with intentional auth, billing, upload, tenant, SQL, secrets, and XSS
  risks for screenshots and recordings.
- `scripts/capture_demo_assets.py` generates repeatable public SVG assets and a
  transcript from `examples/launchpad-saas`.
- Checked-in demo assets cover doctor, status, mock scan, first-run chat, a scan
  flow GIF, and a finding narrative with a visible finding ID.
- The public status demo is seeded from checked-in worker fixtures, so it shows
  open findings, a fix-first recommendation, and exact fix/feedback commands
  without depending on live Claude/Codex quota.
- Real Claude/Codex scan fallback should be exercised against a private test repo
  before launch. With Claude quota unavailable, Codex should complete the focused
  launch-risk scan or report a clear worker diagnostic.
- Model-worker planning excludes local secret material such as `.env`, private
  keys, and PEM files before copying files into isolated worker directories.
- Offline worker-report fixtures prove the executor can surface expected
  launch-risk findings for auth, payments, uploads, tenant isolation, SQL, and
  XSS without depending on live Claude/Codex in CI.

## Market-Ready Gates

- Onboarding: a new user can install with one shell command, run `swain` inside
  a repo, complete setup, scan, and know what to do next without reading source
  code. `swain demo` remains available as the no-quota proof path.
- Reliability: scans degrade gracefully when Claude is out of quota by using Codex
  fallback or clear diagnostic messages.
- Preflight: users can run `swain doctor` before a scan and get clear next steps
  for missing profiles, unavailable workers, invalid playbooks, or tracked
  generated artifacts.
- Actionability: every surfaced finding has an ID, location, impact, confidence,
  and either a fix path or a feedback path.
- CLI handoff: `swain status` can tell a non-expert what to fix first from the
  latest scan without requiring JSON or TUI history spelunking.
- Trust: Swain never writes to the target repo during scans, and patch generation
  returns reviewable diffs instead of applying changes silently.
- Focus: built-in checks cover launch-critical app surfaces: auth, payments,
  uploads, secrets, XSS, SQL/data access, and tenant boundaries.
- Packaging: a built wheel includes the CLI, schemas, bundled playbooks, and
  public demo with no tracked/generated bytecode.
- CI hygiene: `ruff check scripts src tests examples` and `pytest -q` pass
  cleanly.

## Next Gaps

- Run the release checklist from a clean checkout and archive the outputs.
- Run the release checklist from a fresh clone on another machine before
  publishing.
- Run one real worker smoke against a non-public test repo and archive the output
  locally without committing it.
